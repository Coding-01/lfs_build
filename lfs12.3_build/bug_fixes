# 引导修复 (GRUB/UEFI) --- 解决"系统进不去"
这是最常见的"救砖"场景
1. 准备： 使用一个 Linux LiveCD（如 Ubuntu USB）启动电脑。

2. 挂载原系统： * sudo mount /dev/sda2 /mnt (假设 sda2 是你的根分区)
sudo mount /dev/sda1 /mnt/boot/efi (如果是 UEFI 模式，必须挂载 EFI 分区)。

3. 绑定系统资源： 这一步是让 LiveCD 的内核接管原系统硬件
for i in /dev /dev/pts /proc /sys /run; do sudo mount -B $i /mnt$i; done

4. 进入原系统： sudo chroot /mnt

5. 重装并更新GRUB：
  grub-install /dev/sda
  update-grub (或者 grub-mkconfig -o /boot/grub/grub.cfg)

6. 退出并重启：exit 然后 reboot


# 内核崩溃修复 (Kernel Panic) --- 解决"启动报错"
1. 当看到屏幕出现 Kernel Panic - not syncing 时：
    - 修改启动项：在GRUB菜单按e编辑，在linux行末尾添加init=/bin/bash 或 single 模式
2. 检查Initramfs：很多时候是驱动没打进启动镜像
    - 使用命令 dracut -f (CentOS 系) 或 update-initramfs -u (Debian系) 重新生成初始化内存盘
3. 排查驱动冲突：如果是新装了某个 .ko 驱动导致崩溃，进入 /etc/modprobe.d/，把那个驱动名字写进 blacklist.conf(黑名单),禁止它加载
4. 检查磁盘错误：执行 fsck -y /dev/sda2 修复文件系统


# 精简内核(Kernel Stripping) --- 打造"极致性能"
1. 获取当前驱动：在你打算优化的机器上跑一段时间业务，确保所有硬件驱动都加载了
2. 生成精简配置：在内核源码目录下执行 make localmodconfig。它会对比当前系统加载的模块，把没用的全部设为N
3. 手动微调：执行 make menuconfig
    - 去掉不需要的文件系统（如你不用XFS就删掉它）
    - 去掉不用的网络协议（如Amateur Radio support等）
4. 编译与压缩：make -j$(nproc)
5. 去除调试信息(关键): 编译完成后，执行 strip --strip-debug vmlinux, 这能让内核镜像体积大幅缩小


# 引导修复：EFI vs MBR 全过程
这是两种完全不同的逻辑，救砖时必须先判断:
root@lfs:~$ [ -d /sys/firmware/efi ] && echo EFI || echo MBR
MBR             # 这一行是回显
## MBR(Legacy)修复流程：
MBR比较老,逻辑是把启动代码写在磁盘的第一个扇区(512字节)
  1. 挂载： mount /dev/sdXy   /mnt  (根分区)
  2. 挂载系统环境： for i in /dev /proc /sys; do mount -B $i /mnt$i; done
  3. 进入环境：chroot /mnt
  4. 安装GRUB: grub-install /dev/sdX (注意:这里是整个磁盘sda，不是分区sda1)
  5. 生成配置: update-grub

## EFI(UEFI)修复流程：
EFI更现代，它不写扇区，而是把启动文件（.efi）存放在一个 FAT32 分区里。
1. 挂载: 必须挂载根分区 和 EFI 分区
  mount /dev/sdXy  /mnt  (根)
  mount /dev/sdXz  /mnt/boot/efi (EFI分区)
2. 挂载系统环境： 同上，但要多挂载一个 efivarfs（关键！否则报错找不到 EFI 变量）：
  mount -t efivarfs efivarfs /sys/firmware/efi/efivars
3. 进入环境: chroot /mnt
4. 安装GRUB: grub-install --target=x86_64-efi --efi-directory=/boot/efi --bootloader-id=LFS
5. 更新: update-grub
